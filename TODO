- Work on more syntax/parsing
- Move the keyname replacements over to a yaml file...etc.
- Ensure if there are duplicate strings on one line, to handle that correctly? not sure if that's even a use-case?
  - for eg: find_text("%a{'href' => 'http://whatever'} whatever"), ["whatever"]
